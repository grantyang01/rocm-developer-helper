#!/usr/bin/bash

# branch of git@github.com:Kirpich30000/MISA.git
misa_branch=fix_wrw_applicability

# root of shisa source code 
shisa_root=/home/gryang/work/shisa

# work directory
work_root="$(realpath .)/workdir-${misa_branch}"

# misa test 
RESDIR_PARTIAL="${work_root}/results_partial"
RESDIR="${work_root}/results_base"

TEST_CASES="${shisa_root}/testing/misa_test"
#TEST_CASES="$bin_dir/misa_new_cases"
REF_CACHE="$SHISA/ref_cache"
